#video_embed_type_selector {
  float:left;
  margin:0 0 30px;
}

#video_embed_type_selector .radio_wrap {
  float:left;
  margin-right:20px;
}

a#demo_urls {
  padding:10px 16px;
  font-size:20px;
  float:right;
  position:absolute;
  top:115px;
  right:50px;
}

#content #video_code_form .content_left {
  padding-right:17px;
}

/* Lightbox */
a.zoomable {
  display:block;
/*  width:180px;
  height:76px;
*/  background:#000;
  position:relative;
  -webkit-box-shadow:rgba(0,0,0,0.4) 0 4px 10px;
  -moz-box-shadow:rgba(0,0,0,0.4) 0 4px 10px;
  box-shadow:rgba(0,0,0,0.4) 0 4px 10px;
}

a.zoomable span.zoom_icon {
  display:block;
  width:100%;
  height:100%;
  position:absolute;
  top:0;
  left:0;
  background:url(<%= asset_data_uri 'my/sv_zoom_icon.png' %>) no-repeat bottom right;
  opacity:0.3;
  -o-transition:opacity 0.25s;
  -moz-transition:opacity 0.25s;
  -webkit-transition:opacity 0.25s;
  transition:opacity 0.25s;
}

a.zoomable:hover span.zoom_icon {opacity:1}

#code .warning {
  color:red;
  font-weight:bold;
}

a.reset {
  color:#7f8ca9;
  font-weight:bold;
  border-bottom:1px solid #b9c3d8;
  text-decoration:underline;
}

#video_data_name p.note {
  display:block;
  padding:2px 0 2px 22px;
  margin-top:15px;
  font-size:13px;
  font-weight:600;
  line-height:16px;
  background:url(<%= asset_data_uri 'my/note_icon.png' %>) no-repeat;
}

pre.prettyprint {
  border:0;
}