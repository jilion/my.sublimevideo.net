<% if @period.get('type')?: %>
  <span class='day'>
    <%= Highcharts.dateFormat('%e %b %Y', @period.startTime()) %> 
  </span>
  ,
  <span class='time'>
    <%= Highcharts.dateFormat('%H:%M:%S', @period.startTime()) %> 
  </span>
  –
  <span class='day'>
    <%= Highcharts.dateFormat('%e %b %Y', @period.endTime()) %> 
  </span>
  ,
  <span class='time'>
    <%= Highcharts.dateFormat('%H:%M:%S', @period.endTime()) %> 
  </span>
<% end %>