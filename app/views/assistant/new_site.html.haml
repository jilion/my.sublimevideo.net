- title 'Add a site'
= content_header 'Setup assistant'

= render 'assistant/step_guide', current_step: 'new_site'

#site.new.assistant
  %h3.assistant_title On which site do you need a SublimeVideo player?
  = form_for @site, url: url_for, html: { class: 'full' } do |f|
    #site_setup
      .entry.site
        .left
          = f.label :hostname, '', class: "icon"
          = f.text_field :hostname, class: "text", placeholder: "please-edit.me", autocomplete: :off
        %p.desc Add your domain or subdomain.
        .spacer
        %p.note
          = raw "By default the player will also work on the following local development domains: #{content_tag(:em, "localhost")} and #{content_tag(:em, "127.0.0.1")}."
          You will be able to edit them as well as add alias and staging domains later in this site' Settings page.

    = render 'assistant/next_box', display_exit: false
