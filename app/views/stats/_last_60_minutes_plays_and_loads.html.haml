#last_60_minutes_plays{ data: { sparkline_data: stats_presenter.last_60_minutes_plays.map { |s| s[1] } } }
  .sum
    %strong= stats_presenter.last_60_minutes_plays.sum { |s| s[1] }
    plays
  .sparkline
.spacer

#last_60_minutes_loads{ data: { sparkline_data: stats_presenter.last_60_minutes_loads.map { |s| s[1] } } }
  .sum
    %strong= stats_presenter.last_60_minutes_loads.sum { |s| s[1] }
    loads
  .sparkline
