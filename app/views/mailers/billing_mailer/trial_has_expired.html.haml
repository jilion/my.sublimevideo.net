%p
  = raw "Your #{BusinessModel.days_for_trial}-day free trial for the #{@item.title} #{@item.kind_for_email}"
  - if @site.hostname?
    = raw "(for #{link_to @site.hostname, url_with_protocol(@site.hostname)})"
  %strong has now expired.

%p= raw "The #{@item.kind_for_email} has been disabled."

%p= raw "You can <strong>#{link_to('activate it', site_addons_url(@site, h: @item.highlight_param_for_addons_page))}</strong> at any time via the #{link_to('Add-ons', site_addons_url(@site, h: @item.highlight_param_for_addons_page))} page."
