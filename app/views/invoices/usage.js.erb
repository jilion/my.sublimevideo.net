$('current_usage_amount').update('<%= escape_javascript(
  invoice_dates(@invoice) +
  ": " +
  content_tag(:strong, display_amount(@invoice.amount)) +
  " (" +
  link_to("view details", usage_invoices_path, :target => "blank") +
  ")"
).html_safe %>');