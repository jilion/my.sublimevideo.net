%h4= "Plan - #{plan_invoice_item.item.name.humanize} (#{display_amount(plan_invoice_item.price)} per month)"
%table{ :border => 1 }
  %tr
    %td.date
      %span= invoice_item_dates(plan_invoice_item)
    %td.calculation
      %span= "#{display_percentage(plan_invoice_item.percentage)} of usage"
    %td.amount
      %span= display_amount(plan_invoice_item.amount)