%table{ :border => 1 }
  %tr
    %td.date
      %span= invoice_item_dates(addon_invoice_item)
    %td.calculation
      %span= "#{display_percentage(addon_invoice_item.percentage)} of usage"
    %td.amount
      %span= display_amount(addon_invoice_item.amount)