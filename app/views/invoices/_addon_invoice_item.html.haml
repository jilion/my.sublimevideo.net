%table{ :border => 1 }
  %tr
    %td.date
      %span= invoice_item_dates(addon_invoice_item)
    %td.calculation
      %span= "#{display_amount(addon_invoice_item.price)} per month x #{display_percentage(addon_invoice_item.percentage)} ="
    %td.amount
      %span= display_amount(addon_invoice_item.amount)