= title_and_content_header("Usage statement: #{invoice_dates(@invoice)}")
%p.notes= "This is a statement of current usage, not an invoice. It is provided for your reference; at the end of the month, your usage statement will be snapshotted into an invoice."
%table#header
  %tr
    %td.our_info
      = render "our_billing_info", :invoice => @invoice
      .invoice_info
        %span.stamp{ :class => @invoice.state }= @invoice.state
    %td.user_info
      %h4= "Bill to:"
      = render "user_billing_info", :user => @invoice.user

= render "sites", :invoice => @invoice
