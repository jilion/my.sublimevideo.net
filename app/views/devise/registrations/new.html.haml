- content_for :title do
  = "Sign up & get Sublime Video"
  
%h2 Sign up & get Sublime Video

- resource.orders.build(:product_id => Product.find_by_name('video').id) unless resource.orders.any?(&:new_record?)
- form_for(:user, resource, :url => registration_path(resource_name)) do |f|
  = f.error_messages
  
  %p= f.label :full_name
  %p= f.text_field :full_name
  
  %p= f.label :email
  %p= f.text_field :email
  
  %p= f.label :password
  %p= f.password_field :password
  
  %p= f.label :password_confirmation
  %p= f.password_field :password_confirmation
  
  %p= f.submit "Sign up"
  
= render :partial => "devise/shared/devise_links"