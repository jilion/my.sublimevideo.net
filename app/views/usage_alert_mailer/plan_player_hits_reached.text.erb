Hi,

You have reached the <%= @site.plan.name.humanize %> plan's monthly limit for <%= @site.hostname %>.

We advise you to upgrade <%= @site.hostname %> to the next plan (<%= @site.plan.next_plan.name.humanize %>) in order to avoid any overages at the end of the month.

Click on the link below to choose a new plan for <%= @site.hostname %>:

https://<%= ActionMailer::Base.default_url_options[:host] %>/sites/<%= @site.to_param %>/edit#change_plan

The SublimeVideo team