- default_buttons = params[:settings_template][:buttons][:item][:default]
- current_buttons = params[:settings].try(:[], 'social_sharing').try(:[], :buttons) || default_buttons
- id = "kit_setting-#{params[:addon].name}-#{params[:setting_key]}"
- value = current_buttons.join(',')

%h5= t("kit.#{params[:addon].name}.settings.#{params[:translation_key] || params[:setting_key]}")

#sharing_button_sort.sort
  %ul#social_sharing_active_buttons.drop_zone.connectedSortable
    - current_buttons.each do |button|
      %li{ data: { value: button } }= button.titleize

  %div.spacer
  %p.drag_note= "Drag and drop buttons above to enable them and order as you want they appear."

  %ul#social_sharing_inactive_buttons.drop_zone.connectedSortable
    - (default_buttons - current_buttons).each do |button|
      %li{ data: { value: button } }= button.titleize

= hidden_field_tag "kit[addons][#{params[:addon].name}][#{params[:setting_key]}]", value, id: id, class: 'previewable', data: (params[:data] || {}).merge(addon: params[:addon].kind, setting: params[:setting_key], default: value)

%div.spacer
