%h3
  = "Embed code for"
  %strong= truncate(@video.title, :length => 18)
%p.desc= raw "Copy the code below, and paste it into the pages you want to display the video. Make sure you have previously added the SublimeVideo JavaScript code in your page (you can find it in the #{link_to 'Sites', sites_path} page)."
%form
  .size
    %label= "Size:"
    %input#video_width.text.small{ :type => "text", :name => "width", :value => current_user.default_video_embed_width }
    = "x"
    %input#video_height.text.small{ :type => "text", :name => "height", :value => @video.height_from_width(current_user.default_video_embed_width) }
  .embed_code
    %textarea{ :id => 'video_embed_code', :readonly => true, :onclick => "this.focus();this.select();" }= video_tags_for(@video, :width => current_user.default_video_embed_width, :height => @video.height_from_width(current_user.default_video_embed_width))
