%h3
  = "Embed code for"
  %strong= truncate(@video.title, :length => 18)
%p.desc= raw "Copy the code below, and paste it into the pages you want to display the video. Make sure you have previously added the SublimeVideo JavaScript code in your page (you can find it in the #{link_to 'Sites', sites_path} page)."
%form
  .size
    %label= "Size:"
    %input#video_width.text.small{ :type => "text", :name => "width", :value => 600 }
    = "x"
    %input#video_height.text.small{ :type => "text", :name => "height", :value => 255 }
  .embed_code
    %textarea{ :id => 'video_embed_code', :readonly => true, :onclick => "this.focus();this.select();" }= video_tags_for(@video, :width => 600, :height => 255)
