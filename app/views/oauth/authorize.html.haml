%h1 Authorize access to your account
%p
  Would you like to authorize the application
  = link_to "'#{@token.client_application.name}'", @token.client_application.url
  (
  = link_to @token.client_application.url, @token.client_application.url
  ) to access your account?

= form_tag authorize_url do
  = hidden_field_tag("oauth_token", @token.token)
  = hidden_field_tag("oauth_callback", params[:oauth_callback]) if params[:oauth_callback]
  = hidden_field_tag("authorize", "1")

  %p
    = submit_tag "Authorize"
    = " | "
    = link_to "Cancel", params[:redirect_uri]
