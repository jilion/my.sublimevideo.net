= title_and_content_header("Edit the application “#{@application.name.titleize}”")

= form_for @application do |f|
  = render 'form', f: f
  = submit_tag "Update"
  = " | "
  = link_to 'Back', [:client_applications]
