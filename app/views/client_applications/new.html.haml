= title_and_content_header("New OAuth Application")

= form_for :client_application, :url => { :action => :create } do |f|
  = render 'form', :f => f
  = submit_tag "Register"
  = " | "
  = link_to 'Back', [:client_applications]
