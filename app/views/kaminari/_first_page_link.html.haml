-#  Link to the "First" page
-#  available local variables
-#    url:           url to the first page
-#    current_page:  a page object for the currently displayed page
-#    total_pages:     total number of pages
-#    per_page:      number of items to fetch per page
-#    remote:        data-remote
- link_options = params.merge(page: current_page, format: nil)
- html_options = { onclick: 'SublimeVideo.UI.Table.showSpinner(); $(document.body).animate({ scrollTop: 0 });' }

%span.page.first
  = link_to_unless current_page.first?, current_page, link_options, html_options
