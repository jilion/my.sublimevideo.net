-#  "Previous" with link
  - available local variables
    url:           url to the previous page
    current_page:  the page number of currently displayed page
    num_pages:     total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
%span.prev
  = link_to raw(t 'views.pagination.previous'), params.merge(:page => page, :format => nil), :remote => true, :onclick => "MySublimeVideo.showTableSpinner()", :class => 'prev', :rel => 'prev'
