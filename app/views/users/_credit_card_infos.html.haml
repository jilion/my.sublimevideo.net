#credit_card_summary
  %h3.section_title Billing Info

  %h4 Billing Address

  %p
    / Billing address here

  %p= link_to "#{current_user.cc? ? 'Update' : 'Register'} billing address", [:edit, :credit_card]

  %h4 Credit Card
  - if current_user.cc?
    - if current_user.cc_expired?
      %p.alert= t("user.credit_card.expired_long")
    %p.card{ :class => current_user.cc_type }<
      = raw "#{t("user.credit_card.type.#{current_user.cc_type}")} ending in #{content_tag(:strong, current_user.cc_last_digits)}"
    %p
      - if current_user.cc_expired?
        Expired
      - else
        Will expire
      = " on: #{l(current_user.cc_expire_on, :format => :month_fullyear)}"
  - else
    %p No credit card on file.

  %p= link_to "#{current_user.cc? ? 'Update' : 'Register'} credit card", [:edit, :credit_card]

  - if current_user.balance > 0
    %h3.section_title Balance
    Current balance:
    %strong= display_amount(current_user.balance)
    %p
      = raw "You have a balance due to the recent #{link_to 'SublimeVideo pricing changes', ''}."
      This balance will be automatically used for all your further invoices until it's empty.