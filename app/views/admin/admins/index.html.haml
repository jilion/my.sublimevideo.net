= title_and_content_header("Admins")

= link_to "Invite an admin", new_admin_invitation_path, :class => "add"

%table.main#admins
  %tr
    %th.title
      .wrap
        %strong
          = "Email"
    %th= "Confirmed?"
    %th.delete= "Delete"
  = render @admins