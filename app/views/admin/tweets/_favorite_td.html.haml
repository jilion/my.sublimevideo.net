%small
- if has_role?('twitter')
  .favorite_form
    = form_tag [:favorite, :admin, tweet], method: :patch, remote: true do
      = submit_tag "#{tweet.favorited? ? "★" : "✩"}", onclick: "$('#table_spinner').show();"
  .spacer
- else
  = tweet.favorited? ? "★" : "✩"
