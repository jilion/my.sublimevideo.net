%small
- if octave?
  .favorite_form
    = form_tag [:favorite, :admin, tweet], :method => :put, :remote => true do
      = submit_tag "#{tweet.favorited? ? "★" : "✩"}", :onclick => "$('table_spinner').show();"
  .spacer
- else
  = tweet.favorited? ? "★" : "✩"
