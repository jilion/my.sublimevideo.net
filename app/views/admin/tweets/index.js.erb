$('tweets_table_wrap').update("<%= escape_javascript(render 'tweets') %>");
$('table_spinner').hide();
$('users_title').update("<%= pluralize(@tweets.count, 'Tweet') %>");
$(document.body).scrollTo();
