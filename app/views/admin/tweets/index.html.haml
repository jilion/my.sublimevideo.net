- title("Tweets")
= content_header(admin_tweets_page_title(@tweets), id: "tweets_title")

-# .search_form
-#   = form_tag [:admin, :tweets], method: :get, id: 'search_form' do
-#     = text_field_tag :search, '', value: params[:search], id: 'search_input', placeholder: "Search", type: "search", autofocus: true, autosave: "admin_search", results: "5"
-# .spacer

= render 'filter_links'

%p= "Please note: When you favorite a tweet, it's automatically added to the @sublimevideo favorites tweets."
#tweets_table_wrap= render 'tweets'
