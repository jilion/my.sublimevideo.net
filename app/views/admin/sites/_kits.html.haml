%h3 Kits

- if site.kits.empty?
  %p= "No kits yet!"
- else
  %ul
  - site.kits.includes(:design).each do |kit|
    %li
      = kit.name
      = " [#{kit.design.title}]"
      = ' (default)' if site.default_kit == kit
