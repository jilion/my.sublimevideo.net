#site_settings.settings
  %h4.title Production domains
  %p Stats are activated for these domains (if Realtime Stats addon subscribed).

  .entry.site
    = f.label :hostname
    = f.label :hostname, '', class: 'icon'
    = f.text_field :hostname, class: 'text', autocomplete: :off
    %p.info
      This is the main domain where you're using SublimeVideo.
      = raw "#{link_to 'Learn more', docs_url('site-settings#main-domains'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  .entry.site
    = f.label :extra_hostnames
    = f.label :extra_hostnames, '', class: 'icon'
    = f.text_field :extra_hostnames, class: 'text', placeholder: 'yourdomain-alias.com', autocomplete: :off
    %p.info
      = raw "Add alias domains separated by commas. Alias domains are meant to be used #{content_tag(:strong, "exclusively")} for multiple domain names leading to the #{content_tag(:strong, "same content")})."
      = raw "#{link_to 'Learn more', docs_url('site-settings#alias-domains'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  %h4.title Staging and Development domains
  %p Stats aren't activated for these domains

  .entry.site
    = f.label :staging_hostnames
    = f.label :staging_hostnames, '', class: 'icon'
    = f.text_field :staging_hostnames, class: 'text', placeholder: 'yourdomain-staging.com', autocomplete: :off
    %p.info
      = raw "Add staging domains separated by commas."
      = raw "#{link_to 'Learn more', docs_url('site-settings#staging-hostnames'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  .entry.site
    = f.label :dev_hostnames
    = f.label :dev_hostnames, '', class: 'icon'
    = f.text_field :dev_hostnames, class: 'text', placeholder: '127.0.0.1, localhost', autocomplete: :off
    %p.info
      You can use this field to specify local development domains separated by commas.
      = raw "#{link_to 'Learn more', docs_url('site-settings#dev-domains'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  %h4.title Advanced settings

  .entry.path
    = f.label :path
    = f.label :path, '', class: 'icon'
    = f.text_field :path, class: 'text small_icon', placeholder: 'path', autocomplete: :off, maxlength: 256
    %p.info
      = "Restrict your player to a specific path. The path option is only added to production domains."
      = raw "#{link_to 'Learn more', docs_url('site-settings#path'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  .entry.wildcard
    = f.label :wildcard, 'Wildcard (*.yourdomain.com)'
    = f.check_box :wildcard
    = f.label :wildcard, 'Enable'
    %p.info
      = "Enable wildcard to all domains (Staging and Development domains included)."
      = raw "#{link_to 'Learn more', docs_url('site-settings#wildcard'), onclick: 'window.open(this); return false', class: 'hl'}."
  .spacer

  = link_to docs_page_url('site-settings'), class: 'help', onclick: 'window.open(this); return false' do
    %span Read more
