$('sites_table_wrap').update("<%= escape_javascript(render 'sites') %>");

// Also reload the Add site form (in case it had errors that now need to be cleared)
$('new_site').replace("<%= escape_javascript(render 'new') %>");
