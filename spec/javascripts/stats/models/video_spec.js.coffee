describe 'MSVStats.Models.Video', ->

  describe 'mobileSource()', ->
    beforeEach ->

    it 'returns first source if no mobile source', ->
      # @video = new MSVStats.Models.Video
      #   s:
      #     source11:
      #       u: 'http://videos.sublimevideo.net/source11.mp4'
      #       q: 'base'
      #       f: 'mp4'
      #       r: '460x340'
      # expect(@video.mobileSource()).toEqual('http://videos.sublimevideo.net/source11.mp4')
