en:
  time:
    formats:
      date: "%B %e, %Y"
      named_date: "%a, %e %b %Y"
      minutes_timezone: "%d %b %Y, %R %Z"
      seconds_timezone: "%d %b %Y, %R:%S %Z"
      seconds_timezone_y: "%d %b %y, %R:%S %Z"
      month_fullyear: "%B %Y"
      Y_m_d: "%Y/%m/%d"
      d_b_Y: "%d %b %Y"
      year_month_day: "%Y %b %d"

  date:
    formats:
      default: "%B %e, %Y"
      small:   "%m/%d/%Y"
      month_fullyear: "%B %Y"

  number:
    format:
      separator: "." # US separator
      delimiter: "," # US delimiter

  site:
    status:
      payment_issue: "Payment issue, click to review your invoices and retry the payment."
      ok: "Everything runs smoothly, it's ok!"
      in_progress: "Your player is being deployed..."
