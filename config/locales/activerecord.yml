en:
  activerecord:
    attributes:
      site:
        hostname:        "Domain"
        dev_hostnames:   "Development domains"
        extra_hostnames: "Extra domains"
      user:
        terms_and_conditions: "Terms & Conditions"
        cc_type:      "Card type"
        cc_number:    "Card number"
        cc_expire_on: "Expiration date"
        cc_full_name: "Name on card"
        cc_verification_value: "CSC"
        company_employees: "Company size"
        company_videos_served: "Nr. of videos served"
        
    errors:
      models:
        site:
          attributes:
            base:
              at_least_one_domain: "Please set at least a development or an extra domain"
              credit_card_needed:  "You need a valid credit card in order to activate this site"
            hostname:
              invalid:  "is invalid"
              wildcard: "is invalid, please check wildcard option instead"
              taken:    "You have already registered this domain"
            dev_hostnames:
              invalid: "are invalid"
              wildcard: "are invalid, please check wildcard option instead"
              duplicate: "must be unique"
              include_hostname: "cannot include site domain"
            extra_hostnames:
              invalid: "are invalid"
              wildcard: "are invalid, please check wildcard option instead"
              duplicate: "must be unique"
              include_hostname: "cannot include site domain"
              
        invoice:
          attributes:
            state:
              uniqueness: "'open' should be unique per user"
              
        user:
          attributes:
            use:
              at_least_one_option: "Please check at least one option"
            company_url:
              invalid: "is invalid"
            cc_number:
              invalid: "is invalid" # not used because credit_card errors are not localized